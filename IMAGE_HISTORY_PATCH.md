# 建模图片工作流专属 CLI

## 目标和源码基线

在 `dev/codex-custom` 独立分支修复多轮图片历史导致请求过大、远程压缩无法启动的问题，并交付可单独运行的 macOS Apple Silicon CLI。

官方基线：`1715e55076737158ba61d43158ede504de6d4ce1`。遵循用户选择的最新 main，不退回指南建议的旧标签。参考输入：`/Volumes/新加盘/codex-cli-image-history-patch-guide.md`。

## 修复行为和边界

- 尺寸已合规、超过 1 MiB 的 PNG/WebP 尝试无损重编码，仅在体积变小时采用新编码；PNG 保留 16 位颜色精度、透明度、受支持的 ICC 和 EXIF。
- JPEG 维持原有尺寸处理，不新增有损重编码；小图维持原行为。
- 每次请求的 Base64 内联图片最多 12 MiB（含 data URL 前缀）、最多 8 张。最新一条真实用户消息的最后 4 张图先参与保留，其余按从新到旧保留；优先级不会绕过硬上限。
- 超限图片只在发送副本中替换成文字提示。工具调用和输出项、其他文本、持久化历史保持原结构。单张超限图也可能被省略，需要裁剪或缩小后重新查看。
- 普通 Responses、远程压缩、WebSocket 和 HTTP 回退使用同一个入口。替换旧图片后，现有 WebSocket 比较逻辑会拒绝不再匹配的增量历史，发送受预算约束的完整请求。
- 预算约束的是内联图片，不是完整 HTTP 正文；超大文本、音频及服务端通过远程 URL 获取的图片另有边界。

模块职责：`utils/image` 负责单图编码；`core/request_image_budget` 负责请求图片保留顺序和累计预算；`core/client` 在传输方式选择前使用预算；`core/context/image_budget_omission` 定义给模型的省略提示。请求策略属于现有 core 客户端，未增加跨层依赖、配置项或协议字段。

## 使用和回退

安装完成后运行 `codex-patched`，命令行用法与 `codex` 相同。

它使用正常的本地配置和登录状态。已有会话仍可用，但首次验证建议新建会话，逐步查看参考图，再进行一次 `/compact`。

长期比较细节时，模型可能需要重新查看旧图；本地原图和历史不会被补丁删除。

官方 `codex` 和 Desktop 内置组件不被替换。回退直接运行原来的 `codex`，不要删除 `~/.codex`。

## 验证与交付记录

- 单图重编码：先复现失败，再验证修复；16 项图片库测试通过。
- 累计预算、优先级、工具输出结构、确定性和非内联图片：7 项相关核心测试通过。
- 普通/压缩请求与 HTTP 回退：已通过；32 张图的 WebSocket 压力用例通过，验证字节/数量上限和增量失效。
- 图片库与 core 的 scoped Clippy、格式化已完成。
- 第一轮 core 测试：4335 项运行，4088 通过（含 2 项重试通过）、243 失败、4 超时，另有 26 项跳过。存在缺少 CLI/MCP/Code Mode 测试程序、网络策略及沙箱启动受限等问题；不能称为全绿。辅助程序已补齐，全面复测暂不扩大本次交付耗时。图片准备及旧压缩预算的 3 项重负载用例在并发构建期间超时，保留为未闭环项。
- macOS arm64 release 包构建成功；已安装 `~/.local/bin/codex-patched`，新登录 shell 可直接找到命令。包版本 `0.0.1-image-budget.1`；上游源码的 CLI 版本输出为 `codex-cli 0.0.0`。
- 真实模型看图验证成功：向 `gpt-6-astra` 发送 1280×820、约 3 MiB 的 PNG，正确回答“图片左半部分是红色，右半部分是蓝色。”
- 原始 npm CLI 保持 `codex-cli 0.154.0`。
- 生产源码提交 `133f1ba987b5e7de9ca498ab505943ec6ad64a33`；构建启动时注入的 Git 标记为 `0880c1a89`，后续两次提交仅包含等效 lint 清理。完整来源记录见包内 `BUILD-INFO.json`。
- 用户真实建模任务验收：待用户使用后反馈，自动化不代表此项已通过。

## 后续事项

本次交付包括编码、请求预算、回归测试、独立安装包与启动验证。全仓库测试按仓库规范需要另行取得用户同意。

Deferred：纯 WebSocket idle-watchdog 问题、超大文本/音频预算、Windows/Linux 二进制、图片预算配置界面、自动选取旧图细节区域。出现对应证据时在本分支后续补丁处理，不把本次图片预算当作所有超时问题的修复。

上游将来引入等效请求字节预算时，应核对普通请求和压缩路径，运行本补丁的回归测试后优先采用上游实现。自定义 12 MiB/8 张限制是本地保护值，不是官方服务端限制。
