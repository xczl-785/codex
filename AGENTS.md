# Codex 源码学习工作区

## 当前任务定位

本仓库当前以学习 Codex 源码架构为主要用途，同时保留能够构建、运行和验证的真实工程基础。

默认工作模式是静态阅读、源码核对、通俗讲解和详细文档沉淀，而不是参与上游贡献、发布或无目的地重构源码。

## 指令与资料的适用顺序

1. 用户在当前任务中的明确要求。
2. 本文件规定的学习工作流和仓库边界。
3. 涉及源码修改或评审时，完整阅读并遵守 [AGENTS.codex-development.md](AGENTS.codex-development.md)。
4. 目标目录中更具体的 `AGENTS.md`；其局部规则在对应子树内优先。

`AGENTS.codex-development.md` 因改名而不会自动加载。任何修改或评审 Codex 源码的任务，都必须在开始实质工作前主动读取它，不能仅依赖本文件的摘要。

## 默认学习方式

- 以当前分支源码、测试和协议定义为事实来源，文档和类比用于帮助理解，不能替代源码核对。
- 先解释职责、设计动机、数据流和失败后果，再引入真实模块、类型和函数名。
- 面对架构问题，固定覆盖：是什么、为什么这样拆、解决什么问题、不这样做会怎样。
- 先建立整体边界和主链路，再深入局部实现；不要从大量零散函数名开始堆砌信息。
- 默认只做静态检查。本机不需要为了学习内容编译或运行整个仓库。
- 不因当前课程没有涉及某个文件，就判断该文件可以从工程中删除。

## 源码定位

仓库根目录存在 `.codegraph/` 时，需要理解或定位代码应优先使用 CodeGraph，再按需要读取具体文件或使用文本搜索：

```powershell
codegraph status .
codegraph explore "要理解的问题或符号"
codegraph node <symbol>
codegraph callers <symbol>
codegraph callees <symbol>
```

源码变化后，先执行 `codegraph sync .` 再依赖索引结论。`.codegraph/codegraph.db` 是可再生本地索引，不提交到 Git。

## 对话与文档沉淀

- 对话是主要学习载体，回答可以充分展开，确保当下能够理解。
- 每轮先用当前源码核对事实，再回答用户；进入下一学习主题前，把已经讲清且值得长期保留的内容沉淀到对应文档。
- `docs/learning/lessons/` 保存详细课程，必须能够脱离聊天记录独立回顾，不是对话的压缩摘要。
- `docs/learning/reference/` 保存稳定、简短的速查资料，不承担完整论证。
- `docs/learning/learning-records/` 保存学习者当前理解、疑问和教学衔接信息，不作为架构事实来源。
- `docs/learning/MAINTENANCE.md` 和 `NOTES.md` 是教学过程输入，不属于普通读者的课程路线。
- 每份课程尽量围绕一个可验证的问题，记录关键入口、调用路径、状态变化、设计理由和必要的源码证据。
- 课程使用中文和通俗表达；专业名词保留准确英文名称，并在首次出现时解释。

学习入口与文档角色见 [README.md](README.md) 和 [docs/learning/README.md](docs/learning/README.md)。

### 接续已有教学

没有继承既有对话上下文的 Agent 在继续课程前，应按以下顺序恢复教学状态：

1. `docs/learning/MISSION.md`：确认长期目标和范围。
2. `docs/learning/MAINTENANCE.md`：确认已完成课程、当前衔接点和下一主题。
3. `docs/learning/NOTES.md`：恢复讲解方式和用户理解基线。
4. `docs/learning/learning-records/README.md` 及最新记录：了解近期认识变化。
5. 上一课与准备开始的下一课：核对具体知识边界。

`roadmap.md` 描述长期范围和推荐方向，`MAINTENANCE.md` 才是当前教学进度与下一步的过程事实来源。两者出现不同粒度的优先项时，以 `MAINTENANCE.md` 的当前衔接点安排下一课。

## 源码修改模式

当任务明确要求修改、评审或验证源码时：

1. 完整阅读 [AGENTS.codex-development.md](AGENTS.codex-development.md)。
2. 查找目标路径下所有适用的嵌套 `AGENTS.md`。
3. 保持学习分支可构建、可运行，不用教学简化替代真实工程行为。
4. 按完整开发规范执行格式化、lint、测试、Schema 或锁文件更新。
5. 用户只要求静态分析或解释时，不擅自扩展为源码修改。

原上游开发规范是当前源码工作的约束来源，本文件只负责在学习模式和开发模式之间正确路由，不复制或弱化其中的细节。

## 仓库治理边界

- `LICENSE` 和 `NOTICE` 是法律与来源资产，不得因学习用途而删除或改名。
- 构建配置、依赖锁文件、测试、代码生成、跨平台实现和模块级 README 默认视为功能资产。
- 上游产品说明保存在 `README.codex-upstream.md`，不再作为本学习分支的当前入口。
- 上游贡献、发布和 GitHub 自动化暂不进入正常学习路线；是否删除或停用必须另行核对其工程影响。
- 文档清理必须区分稳定知识、当前教学过程输入和历史证据，不能用“看起来旧”代替生命周期判断。
